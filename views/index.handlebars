<h1>Burgers!</h1>

<h2>Burgers that have not been eatin!</h2>

<ul>
  {{#each burgers}}
    {{#unless eatin}}
      {{> burgers/burger-block eatin=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been eatin!</h2>

<ul>
  {{#each burgers}}
    {{#if eatin}}
      {{> burgers/burger-block eatin=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>
  <button type="submit">Add Burger</button>
</form>
